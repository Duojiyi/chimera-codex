# verify-no-upstream-ads.ps1 allowlist
# Format: relative/path:pattern:reason
# One hit family per line. Do NOT allowlist whole directories.
# Only fixture URLs and explicit legacy migration/cleanup constants.

# Embedded #[cfg(test)] fixtures in provider import URL parser
crates/codex-plus-core/src/provider_import.rs:jojocode.com:cfg(test) URL-parse fixture; not a production endpoint

# Legacy display names used only for detection/cleanup (Task 6)
crates/codex-plus-core/src/install/mod.rs:Codex++ 管理工具:LEGACY_MANAGER_NAME / legacy shortcut and app bundle constants

# Root README migration guidance for users still on upstream app names
README.md:Codex++ 管理工具:documents legacy macOS app cleanup; not a product display name
README_EN.md:Codex++ Manager:documents legacy macOS app cleanup; not a product display name

# NSIS deletes legacy shortcuts before creating Chimera entries
scripts/installer/windows/CodexPlusPlus.nsi:Codex++ 管理工具:legacy shortcut cleanup only; new CreateShortcut uses Chimera names

# DMG stage README.txt migration steps for old app bundle names
scripts/installer/macos/package-dmg.sh:Codex++ 管理工具:legacy migration instructions embedded in DMG README.txt

# Workflow artifact rename lands in Task 8; NSIS OutFile already ChimeraCodex-
.github/workflows/release-assets.yml:CodexPlusPlus-:legacy zip artifact names pending Task 8 ChimeraCodex rename
.github/workflows/pr-build.yml:CodexPlusPlus-:legacy zip artifact names pending Task 8 ChimeraCodex rename
